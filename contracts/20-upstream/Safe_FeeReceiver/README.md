# Fee-receiver Safe (3-of-4)

- **Address:** `0x8a08D98CBB218fceB318Ecf3aBc1BA43D8A7aB0E`
- **Kind:** GnosisSafe (GnosisSafeL2 v1.3.0) (proxy; logic via delegatecall to impl)
- **Implementation:** `(see below)`
- **Verified (shell):** True

## Role in the graph

TaxProcessor feeReceiver (protocol fee destination). 3-of-4 multisig sharing 3 signers with the launchpad-admin Safe.

## Runtime bytecode

The on-chain runtime bytecode at this address is saved in `runtime-bytecode.hex` (171 bytes). 
